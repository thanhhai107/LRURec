# Core ML and Deep Learning
torch>=2.0.0
transformers>=4.33.0
peft>=0.5.0
bitsandbytes>=0.41.0
pytorch-lightning>=1.9.0
accelerate>=0.21.0

# Data Processing
gdown
numpy>=1.23.0
pandas>=2.0.0
scikit-learn>=1.2.0

# Utilities
tqdm>=4.64.0
yacs>=0.1.8

# Optional: Logging and Experiment Tracking
wandb>=0.15.0
tensorboard

# Hugging Face Hub (for downloading models)
huggingface-hub>=0.16.0

# Additional dependencies that might be needed
safetensors>=0.3.0
tokenizers>=0.13.0
sentencepiece>=0.1.96
packaging>=23.0

# Note: The following were removed from original requirements.txt:
# - System libraries (cuda, openssl, libgcc, etc.) - These are OS-level packages
#   that Windows handles automatically or are included with Python/pip packages
# - Conda-specific packages - Not needed for pip installation on Windows
# - Package dependencies that are automatically installed (like requests, urllib3, etc.)
# - Packages not used in the codebase (like librosa, opencv, etc.)

